import { InteractiveQuestion } from "@metabase/embedding-sdk-react";
import * as React from "react";

const FirstComponent = () => {
  return <InteractiveQuestion questionId={106} />;
};

export default FirstComponent;

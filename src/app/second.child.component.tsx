import { InteractiveQuestion } from "@metabase/embedding-sdk-react";
import * as React from "react";

const SecondComponent = () => {
  return <InteractiveQuestion questionId={107} />;
};

export default SecondComponent;